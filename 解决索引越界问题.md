# 解决索引越界问题

## 🔴 问题症状

```
RuntimeError: CUDA error: device-side assert triggered
Assertion `index out of bounds: 0 <= tl.broadcast_to(tmp30, [XBLOCK]) < 40960` failed.
```

发生在 `execute_continuous_layer_replication` 中。

## 🎯 根本原因

你的配置：
- 使用了 **9层连续复制** (layer 1-9)
- **16个batch** + **大模型** (Qwen3-8B)
- 在切分 `cu_seqlens` 时，某些层的索引计算出现边界问题

具体原因：
1. `split_idx` 或 `token_split_idx` 超出了数组边界
2. `cu_seqlens_q/k` 的偏移计算不正确
3. 连续复制时，状态传递导致索引累积错误

## 🚀 解决方案（3选1）

### 方案1: 应用修复补丁（推荐）⭐

```bash
cd /root/heyiyuan/HBServe

# 应用修复
python fix_index_overflow.py

# 重新运行
python example_replication_autotune.py
```

这会：
- ✅ 添加严格的边界检查
- ✅ 修复 cu_seqlens 切分逻辑
- ✅ 添加输入验证
- ✅ 自动备份原文件

### 方案2: 减少复制层数（临时方案）⚡

修改 `example_replication_autotune.py`:

```python
# 之前（有问题）
for i in range(1, 10):  # 9层
    model.replicate_layer_to_device(i, 'cuda:1', split_ratio=0.5)

# 改为（安全）
for i in range(5, 8):  # 只复制3层
    model.replicate_layer_to_device(i, 'cuda:1', split_ratio=0.5)
    model.enable_replication_autotune(i, beta=0.3, min_ratio=0.2, max_ratio=0.8)
```

**原理**：减少层数可以降低索引错误的概率。

### 方案3: 调整 split_ratio（快速尝试）

修改 `example_replication_autotune.py`:

```python
# 之前
model.replicate_layer_to_device(i, 'cuda:1', split_ratio=0.5)

# 改为
model.replicate_layer_to_device(i, 'cuda:1', split_ratio=0.6)  # 或 0.4
```

**原理**：调整切分比例可能避开边界临界点。

## 📋 详细修复步骤（方案1）

### 步骤1: 运行修复脚本

```bash
cd /root/heyiyuan/HBServe
python fix_index_overflow.py
```

### 步骤2: 验证修复

```bash
# 启用调试日志
export HB_REPLICA_LOG=1
export CUDA_LAUNCH_BLOCKING=1

# 运行测试
python example_replication_autotune.py
```

### 步骤3: 如果仍有问题

```bash
# 查看详细错误
export HB_DEBUG=1
python example_replication_autotune.py 2>&1 | tee debug.log

# 检查日志中的具体值
grep "\[ReplicaGroup\]" debug.log
grep "\[FIX\]" debug.log
```

## 🔍 修复内容说明

### 修复1: 增强边界检查

**之前**：
```python
cu_seqlens_q_a = context.cu_seqlens_q[:split_idx+1].contiguous()
```

**之后**：
```python
if split_idx + 1 <= len(context.cu_seqlens_q):
    cu_seqlens_q_a = context.cu_seqlens_q[:split_idx+1].contiguous()
else:
    cu_seqlens_q_a = context.cu_seqlens_q.contiguous()
    print(f"[FIX] split_idx+1 超出边界")
```

### 修复2: 安全的偏移计算

**之前**：
```python
cu_seqlens_q_b = context.cu_seqlens_q[split_idx:].clone()
cu_seqlens_q_b = cu_seqlens_q_b - cu_seqlens_q_b[0]
```

**之后**：
```python
if split_idx < len(context.cu_seqlens_q):
    cu_seqlens_q_b = context.cu_seqlens_q[split_idx:].clone()
    if len(cu_seqlens_q_b) > 0:
        cu_seqlens_q_b = cu_seqlens_q_b - cu_seqlens_q_b[0]
else:
    # 创建有效的空张量
    cu_seqlens_q_b = torch.tensor([0], ...)
```

### 修复3: 输入验证

在函数开头添加：
```python
# 检查数据一致性
if context.cu_seqlens_q is not None:
    max_token_idx = context.cu_seqlens_q[-1].item()
    actual_tokens = hidden_states.size(0)
    if max_token_idx != actual_tokens:
        # 数据不一致，禁用优化
        return layer(positions, hidden_states, residual)
```

## 🐛 调试技巧

### 1. 启用详细日志

```bash
export HB_REPLICA_LOG=1
export HB_DEBUG=1
export CUDA_LAUNCH_BLOCKING=1
```

### 2. 检查关键变量

在代码中添加打印：
```python
print(f"Layer {i}:")
print(f"  hidden_states.shape: {hidden_states.shape}")
print(f"  cu_seqlens_q: {context.cu_seqlens_q}")
print(f"  split_idx: {split_idx}")
print(f"  token_split_idx: {token_split_idx}")
```

### 3. 使用更小的配置测试

```python
# 测试配置
prompts = prompts[:4]  # 只用4个batch
for i in range(5, 6):  # 只测试1层
    model.replicate_layer_to_device(i, 'cuda:1', split_ratio=0.5)
```

## 📊 不同方案对比

| 方案 | 难度 | 效果 | 时间 | 推荐度 |
|------|------|------|------|--------|
| 应用补丁 | 简单 | 根治 | 1分钟 | ⭐⭐⭐⭐⭐ |
| 减少层数 | 很简单 | 临时 | 30秒 | ⭐⭐⭐ |
| 调整ratio | 很简单 | 可能有效 | 30秒 | ⭐⭐ |

## ✅ 验证修复是否成功

运行后应该看到：

```bash
# 成功的输出
✅ Layer 1 replication completed
✅ Layer 2 replication completed
...
latency: XX.XX throughput: X.XX

# 而不是
❌ RuntimeError: CUDA error: device-side assert triggered
```

## 🎓 预防措施

为了避免将来出现类似问题：

1. **逐步增加层数**
   ```python
   # 先测试1层
   model.replicate_layer_to_device(5, 'cuda:1')
   # 测试通过后再增加
   ```

2. **使用更安全的 split_ratio**
   ```python
   # 避免极端值
   split_ratio = 0.5  # ✅ 安全
   split_ratio = 0.1  # ⚠️  可能有问题
   split_ratio = 0.9  # ⚠️  可能有问题
   ```

3. **限制 batch size**
   ```python
   # 大模型 + 多层复制时
   max_batch = 8  # 而不是16
   ```

## 📚 相关文档

- `NVLINK_OPTIMIZATION.md` - NVLink优化指南
- `optimization_forward.py` - 优化执行逻辑
- `真实模型性能测试指南.md` - 性能测试

## 🆘 如果问题仍然存在

### 选项1: 使用单层复制测试

```python
# 最保守的配置
model.replicate_layer_to_device(5, 'cuda:1', split_ratio=0.5)
# 不启用 autotune
```

### 选项2: 回滚到原始代码

```bash
# 如果应用了补丁但仍有问题
cp HBserve/utils/optimization_forward.py.backup_index_fix \
   HBserve/utils/optimization_forward.py
```

### 选项3: 禁用连续复制

使用普通的 layer replication（不要配置太多连续的层）：

```python
# 只复制几个不连续的层
model.replicate_layer_to_device(3, 'cuda:1')
model.replicate_layer_to_device(7, 'cuda:1')
model.replicate_layer_to_device(11, 'cuda:1')
```

## 💡 最佳实践

1. ✅ **先用小配置测试**（1-2层，batch=4）
2. ✅ **启用调试日志**（`HB_REPLICA_LOG=1`）
3. ✅ **逐步增加规模**
4. ✅ **应用修复补丁**（预防性修复）
5. ✅ **监控GPU内存**（`watch -n 0.5 nvidia-smi`）

---

## 🚀 快速开始

```bash
# 1. 应用修复
python fix_index_overflow.py

# 2. 修改测试配置（保守）
vim example_replication_autotune.py
# 改为: for i in range(5, 8)  # 只3层

# 3. 运行测试
export HB_REPLICA_LOG=1
python example_replication_autotune.py

# 4. 如果成功，逐步增加层数
```

**问题解决了吗？** 如果还有问题，请查看详细日志并提供：
- `cu_seqlens_q` 的具体值
- `split_idx` 和 `token_split_idx`
- `hidden_states.shape`

