# 性能对比工具使用指南

## 📋 工具说明

这套工具用于全面对比不同优化策略的性能，包括：

1. **Baseline** - 无优化（单设备执行）
2. **Layer Replication** - 层复制（数据并行）
3. **Attention Offload** - 注意力卸载
4. **Continuous Replication** - 连续层复制（流水线并行）

## 🚀 快速开始

### 步骤1: 安装依赖

```bash
pip install torch tabulate matplotlib numpy
```

### 步骤2: 运行单个配置测试

```bash
cd /root/heyiyuan/HBServe

# 测试默认配置 (16×1024)
python benchmark_optimizations.py

# 测试自定义配置
python benchmark_optimizations.py --batch_size 32 --seq_len 2048

# 指定设备
python benchmark_optimizations.py --device_a cuda:0 --device_b cuda:1
```

### 步骤3: 运行综合测试（推荐）

```bash
# 测试多种配置，生成完整对比
python benchmark_optimizations.py --comprehensive
```

这将自动测试：
- 小batch: 4×128
- 中batch: 8×512
- 大batch: 16×1024
- 超大batch: 32×2048

### 步骤4: 生成可视化报告

```bash
# 生成图表和Markdown报告
python visualize_benchmark.py
```

生成的文件：
- `latency_comparison.png` - 延迟对比柱状图
- `speedup_heatmap.png` - 加速比热力图
- `throughput_comparison.png` - 吞吐量折线图
- `memory_usage.png` - 内存使用对比
- `BENCHMARK_REPORT.md` - 完整测试报告

## 📊 输出示例

### 终端输出

```
================================================================================
性能测试摘要
================================================================================

配置:
  Batch Size: 16
  Sequence Length: 1024
  Total Tokens: 16384
  Hidden Size: 4096
  Num Layers: 8
  设备: cuda:0 <-> cuda:1

+------------------------+--------------+-------------+---------------+---------+-----------+--------+
| 策略                   | 平均延迟(ms) | 标准差(ms)  | 吞吐量(tok/s) | 加速比  | 内存(GB)  | 状态   |
+========================+==============+=============+===============+=========+===========+========+
| Baseline               | 245.76       | 2.34        | 66,700        | 1.00x   | 4.52      | ✅     |
+------------------------+--------------+-------------+---------------+---------+-----------+--------+
| Layer Replication      | 130.45       | 1.87        | 125,600       | 1.88x   | 8.94      | ✅     |
+------------------------+--------------+-------------+---------------+---------+-----------+--------+
| Attention Offload      | 155.23       | 2.15        | 105,600       | 1.58x   | 6.73      | ✅     |
+------------------------+--------------+-------------+---------------+---------+-----------+--------+
| Continuous Replication | 140.89       | 1.92        | 116,300       | 1.74x   | 8.21      | ✅     |
+------------------------+--------------+-------------+---------------+---------+-----------+--------+

最佳性能:
  最低延迟: Layer Replication (130.45 ms)
  最高吞吐: Layer Replication (125,600 tokens/s)
```

### JSON输出 (benchmark_results.json)

```json
{
  "config": {
    "batch_size": 16,
    "seq_len": 1024,
    "hidden_size": 4096,
    "num_layers": 8,
    "device_a": "cuda:0",
    "device_b": "cuda:1"
  },
  "results": [
    {
      "strategy": "Baseline",
      "avg_latency_ms": 245.76,
      "throughput_tokens_per_sec": 66700,
      "speedup_vs_baseline": 1.0,
      "memory_allocated_gb": 4.52,
      "success": true
    },
    {
      "strategy": "Layer Replication",
      "avg_latency_ms": 130.45,
      "throughput_tokens_per_sec": 125600,
      "speedup_vs_baseline": 1.88,
      "memory_allocated_gb": 8.94,
      "success": true
    }
  ]
}
```

## 🔧 高级用法

### 1. 自定义测试参数

```python
from benchmark_optimizations import BenchmarkConfig, OptimizationBenchmark

# 创建自定义配置
config = BenchmarkConfig(
    batch_size=64,
    seq_len=4096,
    hidden_size=4096,
    num_layers=16,
    num_heads=32,
    num_kv_heads=8,
    device_a="cuda:0",
    device_b="cuda:1",
    num_warmup=5,
    num_iterations=20
)

# 运行测试
benchmark = OptimizationBenchmark(config)
benchmark.run_all_benchmarks()
benchmark.print_summary()
benchmark.save_results("my_benchmark.json")
```

### 2. 只测试特定策略

```python
benchmark = OptimizationBenchmark(config)

# 只测试baseline和layer replication
benchmark.benchmark_baseline()
benchmark.benchmark_layer_replication()

benchmark.print_summary()
```

### 3. 对比NVLink vs PCIe

```bash
# 测试NVLink连接的GPU
python benchmark_optimizations.py \
    --device_a cuda:0 \
    --device_b cuda:1 \
    --batch_size 16 \
    --seq_len 1024

# 测试PCIe连接的GPU (如果有多个不同NUMA节点的GPU)
python benchmark_optimizations.py \
    --device_a cuda:0 \
    --device_b cuda:2 \
    --batch_size 16 \
    --seq_len 1024
```

### 4. 测试不同层数的影响

```bash
# 测试浅层网络
python benchmark_optimizations.py --num_layers 4

# 测试深层网络
python benchmark_optimizations.py --num_layers 32
```

## 📈 结果解读

### 加速比判断标准

| 加速比 | 评价 | 说明 |
|--------|------|------|
| ≥ 1.8x | ✅✅✅ 优秀 | 接近理论上限，强烈推荐 |
| 1.5-1.8x | ✅✅ 很好 | 显著加速，推荐使用 |
| 1.2-1.5x | ✅ 有效 | 有改进，可以使用 |
| 1.0-1.2x | ⚠️ 边界 | 效果不明显，慎用 |
| < 1.0x | ❌ 变慢 | 不推荐，检查配置 |

### 内存使用分析

- **Baseline**: 最小（单设备）
- **Layer Replication**: 约2倍（两个设备各一份模型）
- **Attention Offload**: 约1.5倍（部分模型复制）
- **Continuous Replication**: 约2倍（模型分片）

### 适用场景

#### Layer Replication
- ✅ 大batch（>16）
- ✅ 长序列（>1024）
- ✅ Prefill阶段
- ✅ NVLink连接
- ❌ 小batch（<8）
- ❌ 低延迟要求

#### Attention Offload
- ✅ 中等batch（8-32）
- ✅ 注意力计算占比大
- ✅ 内存受限
- ❌ 超小batch
- ❌ PCIe连接

#### Continuous Replication
- ✅ 深层网络（>16层）
- ✅ 流水线并行
- ✅ 固定batch大小
- ❌ 动态batch
- ❌ 交互式推理

## 🔍 性能调优

### 如果加速比不理想

1. **检查数据传输**
   ```python
   # 确保使用non_blocking
   data.to(device, non_blocking=True)
   ```

2. **调整切分比例**
   ```python
   # 在benchmark代码中修改
   split_ratio = 0.5  # 尝试0.4或0.6
   ```

3. **增大batch size**
   ```bash
   python benchmark_optimizations.py --batch_size 32
   ```

4. **检查GPU利用率**
   ```bash
   # 运行测试时监控
   watch -n 0.1 nvidia-smi
   ```

### 如果内存不足

1. **减少batch size**
2. **使用gradient checkpointing**
3. **降低hidden size**
4. **减少num_layers**

## 📝 生成测试报告

### 完整工作流

```bash
# 1. 运行综合测试
python benchmark_optimizations.py --comprehensive

# 2. 生成可视化
python visualize_benchmark.py

# 3. 查看报告
cat BENCHMARK_REPORT.md

# 4. 查看图表
# 在文件浏览器中打开 *.png 文件
```

### 自定义报告

```python
from visualize_benchmark import (
    load_results,
    plot_latency_comparison,
    generate_markdown_report
)

# 加载结果
results = load_results(['benchmark_results.json'])

# 生成自定义图表
plot_latency_comparison(results, output_file='my_latency.png')

# 生成报告
generate_markdown_report(results, output_file='MY_REPORT.md')
```

## 🐛 故障排查

### 问题1: 找不到GPU

```
❌ RuntimeError: CUDA out of memory
```

**解决**:
- 减小batch_size
- 减小seq_len
- 减小num_layers

### 问题2: 测试结果异常

```
❌ 某些策略显示 N/A
```

**检查**:
- GPU是否真正连接
- NVLink是否启用: `nvidia-smi nvlink --status`
- 设备索引是否正确

### 问题3: 性能不如预期

```
加速比 < 1.2x
```

**排查**:
1. 运行单元测试:
   ```bash
   python -c "
   import torch
   import time
   a = torch.randn(1024, 1024, device='cuda:0')
   start = time.time()
   b = a.to('cuda:1', non_blocking=True)
   torch.cuda.synchronize()
   print(f'传输时间: {(time.time()-start)*1000:.2f}ms')
   "
   ```

2. 检查non_blocking是否生效
3. 增大workload（batch_size, seq_len）

## 💡 最佳实践

1. **先运行综合测试**，了解整体性能
2. **关注加速比热力图**，找出最适合你场景的策略
3. **结合内存使用**，选择性价比最高的方案
4. **在实际模型上验证**，benchmark结果可能与真实场景有差异
5. **定期重新测试**，不同PyTorch版本性能可能不同

## 📚 相关文档

- `NVLINK_OPTIMIZATION.md` - NVLink优化详细指南
- `快速开始_NVLINK优化.md` - NVLink快速上手
- `OPTIMIZATION_FIX.md` - 优化问题修复方案
- `docs/optimization_analysis.md` - 性能问题分析

## 🎯 总结

使用这套工具，你可以：

✅ 快速对比不同优化策略  
✅ 找到最适合你场景的方案  
✅ 生成专业的性能报告  
✅ 可视化分析性能瓶颈  
✅ 指导实际部署决策  

开始测试吧！🚀

